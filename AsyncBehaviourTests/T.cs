using System;

namespace AsyncBehaviourTests
{
    public static class T
    {
        public static TimeSpan LongTime = TimeSpan.FromMilliseconds(200);
        public static TimeSpan MediumTime = TimeSpan.FromMilliseconds(50);
        public static TimeSpan ShortTime = TimeSpan.FromMilliseconds(10);
    }
}